name: Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./uptime-cli
    steps:
      - uses: actions/checkout@v4

      - name: Build docker image
        run: docker build -t uptime-checker . --no-cache


